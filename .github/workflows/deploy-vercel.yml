name: Deploy to Vercel Disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    name: Vercel deployment disabled
    runs-on: ubuntu-latest
    steps:
      - name: Explain
        run: |
          echo "Vercel deployment has been disabled."
          echo "Production deployment now uses .github/workflows/deploy-droplet.yml for DigitalOcean Droplet."
